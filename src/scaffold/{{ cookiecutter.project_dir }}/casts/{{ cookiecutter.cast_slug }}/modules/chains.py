"""Construct LCEL chains required by the {{ cookiecutter.cast_name }} workflow.

Guidelines:
    - Create LangChain Expression Language (LCEL) pipelines.
    - Reuse components defined in `modules.prompts` and `modules.models`.
"""
